{
  "github": {
    "type": "stdio",
    "command": "/home/afidhadra/go/bin/github-mcp-server",
    "args": ["stdio"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
    },
    "timeout": 30000
  }
}
